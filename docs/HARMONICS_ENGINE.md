# HARMONICS Engine (OBSOLETE)

Removed from the shipping product in Phase 2.
Replaced by **TUNE** (see `docs/EFFECT_ENGINE.md`, `docs/SOUND_REGRESSION_AUDIT.md`).
Obsolete APVTS IDs `harmonicsEnabled` / `scaleColor` / `scaleTransient` remain for session compat only.
The former `ScaleAccentuator` 48-band BP design is documented as a resonator-bank risk in the sound regression audit.
